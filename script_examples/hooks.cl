-- BASE HOOK FILE
{
  run_in_room_enter = function(room, char)
  end,

  run_in_room_leave = function(room, char)
  end,

  run_in_exit = function(char, origin, dest)
  end,

  run_on_use = function(char, cmd)
  end,

  run_on_char = function(char, cmd)
  end,
}
